# /plan — vague idea to concrete steps

When the user invokes /plan: read the relevant files first, then reply with
plain text (no tool block) containing: goal in one line, numbered steps each
naming exact files, risks, and what "done" looks like. Do not write code in
/plan mode — planning only.
